namespace Dfe.Academies.Academisation.Core
{
	/// <summary>
	/// Class used to represent a result containing a conditional create of another object,
	/// may be a <see cref="CreateResult"/> if it does, or a CommandResult if it does not.
	/// </summary>
	public abstract class CommandOrCreateResult
	{
	}
}
